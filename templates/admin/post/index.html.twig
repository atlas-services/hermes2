{% extends '/admin/base.html.twig' %}

{% block title %}{{ 'form.label.list_posts'|trans() }}{% endblock %}

{% block content %}
    {{ component('Alert', {class: 'mt-5'}) }}
    <div>
        <h1>{{ 'form.label.list_posts'|trans() }}</h1>
        <a class="btn btn-success" href="{{ path('post_new') }}">{{ 'form.label.create'|trans() }}</a>
        {{ component('Post', {posts: posts}) }}
        <a class="btn btn-success" href="{{ path('post_new') }}">{{ 'form.label.create'|trans() }}</a>
    </div>
{% endblock %}
