<table class="table">
    <thead>
        <tr>
            <th class="col-3">{{ 'form.label.active'|trans() }}</th>
            <th class="col-3">{{ 'form.label.menu'|trans() ~ '/' ~  'form.label.sub_menu'|trans()  }}</th>
            <th class="col-3">{{ 'form.label.position'|trans() }}</th>
            <th>{{ 'form.label.actions'|trans() }}</th>
        </tr>
    </thead>
    <tbody data-controller="move-item" data-type="post" data-locale="{{ app.request.locale }}" >
    {% for post in posts %}
        <tr class="list-items align-middle" data-move-item-target="item" data-item-id="{{ post.id }}"  >
            <td class="col-3">
                <div class="form-check form-switch form-switch-sm my-0">
                    <input data-controller="active-item"  type="checkbox" class="form-check-input bg-success" data-action="click->active-item#switch" {{ post.active  ? "checked" : "" }}>
                    <label class="custom-control-label">{{ post.name }}</label>
                </div>
            </td>
            <td class="col-3">
                <div >
                      <label class="custom-control-label">{{ post.menu.parent.name ~ "/" ~ post.menu.name }}</label>
                </div>
            </td>
            <td  class="col-3">
                <button class="btn btn-outline-success" data-action="click->move-item#moveUp">↑</button>
                <button class="btn btn-outline-success" data-action="click->move-item#moveDown">↓</button>
            </td>
            <td>
                    <a class="py-1 px-2 btn btn-outline-success rounded-3" href="{{ path('post_edit', {'id': post.id}) }}">{{ ux_icon('fa:edit', {height: '16px', width: '22px', color:"#89c3a5"}) }}</a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="5">{{ 'form.label.no_record'|trans() }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
