


{% for type, messages in app.flashes %}
    {% for message in messages %}
    <div class="alert alert-{{ type }} {{ class }}" style="z-index:-1">
        {{ message }}
        {# Same as above, but using "this", which is the component object #}
        {# {{ this.message }} #}
    </div>
    {% endfor %}
{% endfor %}
